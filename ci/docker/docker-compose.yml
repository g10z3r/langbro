version: "3.9"
services:
  langbro_backend:
    build:
      dockerfile: ci/docker/backend.Dockerfile
      context: ../../
    container_name: langbro_backend
    restart: unless-stopped

networks:
  default:
    name: langbro_net
